spring:
  config:
    activate:
      on-profile: dev

  datasource:
    url: jdbc:postgresql://localhost:5432/ticketflow
    username: postgres
    password: postgres

  data:
    redis:
      host: localhost
      port: 6379

logging:
  level:
    com.ticket: DEBUG
